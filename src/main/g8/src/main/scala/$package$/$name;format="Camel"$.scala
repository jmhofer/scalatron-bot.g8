package $package$

class $name;format="Camel"$ {
  // This is the control function of your bot.
  // It will get called by Scalatron on each simulation step.
  def step(in: String): String = {
    "Status(text=Hello World)"
  }
}
